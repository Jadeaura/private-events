<h1>CREATE EVENT</h1>
<%= form_for @event do |f| %>
	<%= f.label :location %>
	<%= f.text_field :location %>

	<%= f.hidden_field :date, :value => (Time.now + 5.minutes) %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit "Create an event!" %>
<% end %>